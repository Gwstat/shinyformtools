% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_migration.R
\name{plan_migration}
\alias{plan_migration}
\title{Plan a database schema migration}
\usage{
plan_migration(conn, form)
}
\arguments{
\item{conn}{A DBI connection.}

\item{form}{Object created with \code{\link[=form]{form()}}.}
}
\value{
A migration plan object.
}
\description{
Plan a database schema migration
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
conn <- db_connect(db)

# On a fresh database the plan contains a single create_table action.
plan <- plan_migration(conn, contacts)
plan$actions$action

db_disconnect(conn)
}
