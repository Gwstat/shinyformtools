% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form.R
\name{form}
\alias{form}
\title{Define a form schema}
\usage{
form(
  form_id,
  fields,
  table_name = form_id,
  db_path = "form_data.sqlite",
  db = NULL,
  form_name = form_id,
  version = 1L,
  schema_policy = c("safe", "manual"),
  on_edit_missing_required = c("warn", "require", "ignore"),
  tab_labels = NULL,
  slide_labels = NULL,
  messages = list(),
  validation_rules = list(),
  header = NULL,
  footer = NULL,
  server = NULL
)
}
\arguments{
\item{form_id}{Stable form identifier.}

\item{fields}{List of objects created with \code{\link[=form_field]{form_field()}}.}

\item{table_name}{Database table name. Defaults to \code{form_id}.}

\item{db_path}{SQLite database path. Defaults to \code{"form_data.sqlite"}.
Kept for backwards compatibility. Prefer \code{db = db_sqlite(...)} for
new code.}

\item{db}{Database backend configuration created with \code{db_sqlite()} or
\code{db_mariadb()} or \code{db_duckdb()}. If omitted, \code{db_path} is used as SQLite path.}

\item{form_name}{Optional user-facing form name.}

\item{version}{Integer-like form schema version.}

\item{schema_policy}{Schema handling policy. Currently \code{"safe"} or
\code{"manual"}.}

\item{on_edit_missing_required}{How to treat new mandatory fields when old
records are edited. One of \code{"warn"}, \code{"require"} or \code{"ignore"}.}

\item{tab_labels}{Optional labels for tab indices. Named labels use the tab
value as name; unnamed labels are interpreted as zero-based layout labels.}

\item{slide_labels}{Optional labels for slide indices. Named labels use the
slide value as name; unnamed labels are interpreted as zero-based layout
labels.}

\item{messages}{Optional named list of validation messages.}

\item{validation_rules}{Optional list of objects created with
\code{\link[=validation_rule]{validation_rule()}} or \code{\link[=required_if]{required_if()}}. Rules are evaluated
server-side during insert and update.}

\item{header}{Optional form header. Can be a HTML string, Shiny UI object, or
function returning Shiny UI. Function arguments may include \code{ns}, \code{prefix},
\code{values} and \code{form}.}

\item{footer}{Optional form footer. Can be a HTML string, Shiny UI object, or
function returning Shiny UI. Function arguments may include \code{ns}, \code{prefix},
\code{values} and \code{form}.}

\item{server}{Optional Shiny server hook with signature
\verb{function(input, output, session)}.}
}
\value{
A form definition object of class \code{"sft_form"}.
}
\description{
Creates a shinyformtools form schema from a list of field definitions.
}
\examples{
# A form is described once; the schema, CRUD and Shiny module derive from it.
contacts <- form(
  form_id = "contacts",
  table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(
    form_field(id = "name", label = "Name", mandatory = TRUE),
    form_field(id = "email", label = "Email", unique = TRUE),
    form_field(
      id = "team", label = "Team", input_type = "selectInput",
      args = list(choices = c("Sales", "Support"))
    )
  )
)
contacts$form_id
}
