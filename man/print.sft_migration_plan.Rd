% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_migration.R
\name{print.sft_migration_plan}
\alias{print.sft_migration_plan}
\title{Print a migration plan}
\usage{
\method{print}{sft_migration_plan}(x, ...)
}
\arguments{
\item{x}{A migration plan from \code{\link[=plan_migration]{plan_migration()}}.}

\item{...}{Ignored.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Print a migration plan
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
conn <- db_connect(db)

plan <- plan_migration(conn, contacts)
print(plan)

db_disconnect(conn)
}
