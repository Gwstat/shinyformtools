% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_registry.R
\name{register_input}
\alias{register_input}
\title{Register a custom form input}
\usage{
register_input(
  name,
  fun,
  value_arg = "value",
  multiple = FALSE,
  update_fun = NULL,
  encode = NULL,
  decode = NULL,
  format = NULL
)
}
\arguments{
\item{name}{Input type name, used as the \code{input_type} string in
\code{\link[=form_field]{form_field()}}. Must not collide with a built-in input type.}

\item{fun}{The input UI function, for example \code{shinyWidgets::knobInput}. It is
called with \code{inputId}, \code{label} and the field's \code{args}.}

\item{value_arg}{Name of the argument that carries the current value when the
input is rendered or updated, typically \code{"value"} or \code{"selected"}. Use
\code{NULL} if the input takes no value argument (the value is then left to
\code{args}).}

\item{multiple}{Logical. When \code{TRUE}, the value is a vector: it is stored as a
JSON array and decoded back to a vector. Ignored when \code{encode}/\code{decode} are
supplied.}

\item{update_fun}{Optional update function (for example
\code{shinyWidgets::updateKnobInput}) used for dynamic values and, if it accepts
a \code{choices} argument, dynamic choices. It is called with \code{session},
\code{inputId} and the value/choices arguments.}

\item{encode}{Optional function mapping an input value to the scalar stored in
the database. Overrides the default text/JSON handling.}

\item{decode}{Optional function mapping a stored value back to the value
handed to the input when a record is edited. Overrides the default.}

\item{format}{Optional function mapping a stored value to the string shown in
the records and versions tables. Overrides the default.}
}
\value{
Invisibly, \code{name}.
}
\description{
Teaches shinyformtools about an additional Shiny input function (for example
one from \pkg{shinyWidgets}) so it can be used as an \code{input_type} in
\code{\link[=form_field]{form_field()}} exactly like a built-in. The widget is rendered in the add and
edit forms, its value is read back, stored, restored and shown in the records
table, and -- when an \code{update_fun} is supplied -- it participates in dynamic
choices/values.
}
\details{
Call \code{register_input()} before the \code{\link[=form_field]{form_field()}} that uses the registered
\code{input_type} (a field is validated against the registry at definition time).
Re-registering the same \code{name} overwrites the previous entry; built-in input
names cannot be overridden.

By default the input is treated as a single-valued text input: its value is
stored verbatim and read back unchanged. For multi-valued inputs set
\code{multiple = TRUE} to store the value as a JSON array and decode it back to a
vector. For full control over how the value is serialized, supply \code{encode}
(value -> stored scalar), \code{decode} (stored value -> input value) and \code{format}
(stored value -> table display string).
}
\examples{
\dontrun{
# A shinyWidgets knob as a numeric input.
register_input(
  "knobInput",
  fun = shinyWidgets::knobInput,
  value_arg = "value",
  update_fun = shinyWidgets::updateKnobInput
)

vol <- form_field(
  id = "vol", label = "Volume", input_type = "knobInput",
  args = list(min = 0, max = 100), db_type = "REAL"
)
}
}
