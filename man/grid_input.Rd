% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_input.R
\name{grid_input}
\alias{grid_input}
\title{A grid of number cells as a Shiny input}
\usage{
grid_input(
  inputId,
  label = NULL,
  value = NULL,
  rows = rownames(value),
  cols = colnames(value),
  hint = NULL,
  min = 0,
  max = NULL,
  step = 1,
  sums = TRUE,
  sum_label = "Sum",
  width = NULL
)
}
\arguments{
\item{inputId}{Input id.}

\item{label}{Optional label above the grid.}

\item{value}{Numeric matrix of starting values, or \code{NULL} for empty cells.
Its dimnames supply \code{rows} and \code{cols} when those are not given.}

\item{rows, cols}{Row and column labels.}

\item{hint}{Optional matrix of the same shape shown greyed inside each cell,
for example yesterday's values. It is a hint only, never the value.}

\item{min, max, step}{Passed to the number cells.}

\item{sums}{Logical. Show row, column and total sums.}

\item{sum_label}{Header of the sum column and row.}

\item{width}{Optional CSS width of the whole grid.}
}
\value{
A Shiny input tag.
}
\description{
Renders a table whose rows and columns are fixed and whose cells are
number inputs, for entering many related values at once (a cross table of
counts, say). Enter and the arrow keys walk the cells, a block copied from
a spreadsheet is pasted from the focused cell onwards, and row, column and
total sums follow the entries. The input's value is a numeric matrix with
the row and column names; an empty cell is \code{NA}. The value updates half a
second after the last keystroke.
}
\details{
\code{grid_input} is also a built-in \code{input_type} for \code{\link[=form_field]{form_field()}}: the matrix
is stored as one JSON text and shown in the records table as its column
totals. Pass \code{rows}, \code{cols} and the other arguments through
\code{form_field(args = list(...))}.
}
\examples{
\dontrun{
library(shiny)
ui <- fluidPage(
  grid_input("votes", "Votes", rows = c("Party A", "Party B"),
             cols = c("First", "Second")),
  verbatimTextOutput("value")
)
server <- function(input, output, session) {
  output$value <- renderPrint(input$votes)
}
shinyApp(ui, server)
}
}
\seealso{
\code{\link[=update_grid_input]{update_grid_input()}}
}
