% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes.R
\name{decode_shape}
\alias{decode_shape}
\title{Decode a stored shape column back to sf geometry}
\usage{
decode_shape(values, field)
}
\arguments{
\item{values}{Character vector of serialized geometries from a shape column.
\code{NA} / empty entries (records with no attached shape) become empty
geometries.}

\item{field}{The shape field definition, from \code{\link[=shape_field]{shape_field()}}.}
}
\value{
An \code{sf} simple-feature geometry list-column (\code{sfc}).
}
\description{
Inverse of the serialization done by \code{\link[=attach_shapes]{attach_shapes()}}. Use it to turn a
fetched shape column into geometry for rendering (for example on a map).
}
\examples{
# Decodes stored WKT/GeoJSON text back into sf geometry (requires the sf
# package, an optional dependency).
\dontrun{
fld <- shape_field("geom", encoding = "wkt", crs = 4326)
values <- c("POINT (0 0)", NA, "POINT (1 1)")
geom <- decode_shape(values, fld)
geom
}
}
