% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field.R
\name{shape_field}
\alias{shape_field}
\title{Define a fixed-geometry (shape) field}
\usage{
shape_field(
  id,
  label = "",
  crs = 4326,
  encoding = c("geojson", "wkt"),
  db_column = NULL,
  show = FALSE,
  tab = 0L,
  tab_label = NULL,
  slide = 0L,
  slide_label = NULL,
  col = 0L,
  pos = 0L
)
}
\arguments{
\item{id}{Stable field identifier and, by default, the database column name.}

\item{label}{User-facing label.}

\item{crs}{Coordinate reference system the geometry is reprojected to on
attach, as anything \code{\link[sf:st_crs]{sf::st_crs()}} accepts (default EPSG:4326, which GeoJSON
requires).}

\item{encoding}{Serialization for the stored text. One of \code{"geojson"}
(default) or \code{"wkt"}.}

\item{db_column}{Database column name. Defaults to \code{id}.}

\item{show}{Logical. Whether the column is offered in the records table.
Defaults to \code{FALSE}, since a serialized geometry is not a useful table cell.}

\item{tab, tab_label, slide, slide_label, col, pos}{Layout position, as in
\code{\link[=form_field]{form_field()}}.}
}
\value{
A field definition object of class \code{"sft_field"}.
}
\description{
Declares a non-editable column that stores a geometry per record, for example
the boundary of an election district. The geometry is loaded out of band with
\code{\link[=attach_shapes]{attach_shapes()}} and is never collected from a form input, validated, or
written by insert/update, so editing a record's other (input) fields never
disturbs its shape. Stored as serialized text (GeoJSON or WKT) in a \code{TEXT}
column, identical across all backends.
}
\examples{
# Declare a geometry column; the geometry itself is loaded later with
# attach_shapes(). Building the field does not require the sf package.
boundary <- shape_field(id = "boundary", label = "District boundary")
boundary$encoding
}
