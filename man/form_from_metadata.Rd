% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_import.R
\name{form_from_metadata}
\alias{form_from_metadata}
\title{Create a form schema from metadata tables}
\usage{
form_from_metadata(
  inputs,
  settings = NULL,
  questionnaire = NULL,
  form_id = NULL,
  table_name = NULL,
  db_path = "form_data.sqlite",
  db = NULL,
  form_name = NULL,
  version = 1L,
  schema_policy = c("safe", "manual"),
  on_edit_missing_required = c("warn", "require", "ignore"),
  messages = list(),
  header = NULL,
  footer = NULL,
  server = NULL
)
}
\arguments{
\item{inputs}{Data frame describing fields. Supported columns include \code{Id},
\code{label}, \code{inputObj}/\code{input_type}, \code{args_json}, \code{mandatory},
\code{toggleunique}/\code{unique}, \code{renamed_from}, \code{db_column}, \code{db_type}, \code{slide},
\code{tab}, \code{col} and \code{pos}.}

\item{settings}{Optional one-row or multi-row settings data frame. If a
questionnaire column is present, it is filtered by \code{questionnaire}.}

\item{questionnaire}{Optional questionnaire/form name used to filter legacy
metadata columns such as \code{Fragebogen} or \code{questionaire}.}

\item{form_id}{Optional form id. Defaults to a safe identifier derived from
\code{questionnaire}.}

\item{table_name}{Optional database table name. Defaults to \code{form_id}.}

\item{db_path}{SQLite path used when \code{db} is omitted.}

\item{db}{Database backend created with \code{\link[=db_sqlite]{db_sqlite()}} or
\code{\link[=db_mariadb]{db_mariadb()}}.}

\item{form_name}{User-facing form name.}

\item{version}{Schema version.}

\item{schema_policy}{Schema handling policy passed to \code{\link[=form]{form()}}.}

\item{on_edit_missing_required}{Handling of new mandatory fields when old
records are edited.}

\item{messages}{Optional validation messages.}

\item{header}{Optional form header.}

\item{footer}{Optional form footer.}

\item{server}{Optional Shiny server hook.}
}
\value{
A form definition object of class \code{form}.
}
\description{
Converts questionnaire metadata to a \code{shinyformtools} form schema. This is a
safe replacement path for legacy generators that created Shiny code with
\code{eval(parse())}.
}
\examples{
# Describe the fields as a metadata data frame, then derive a form from it.
inputs <- data.frame(
  id = c("name", "age"),
  label = c("Name", "Age"),
  input_type = c("text", "numeric"),
  mandatory = c(TRUE, FALSE),
  stringsAsFactors = FALSE
)
frm <- form_from_metadata(
  inputs,
  form_id = "people",
  db = db_sqlite(tempfile(fileext = ".sqlite"))
)
frm$form_id
vapply(frm$fields, function(f) f$id, character(1))
}
