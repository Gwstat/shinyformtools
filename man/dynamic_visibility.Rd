% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_bindings.R
\name{dynamic_visibility}
\alias{dynamic_visibility}
\title{Define dynamic visibility for a form input}
\usage{
dynamic_visibility(field, visible, depends_on = NULL)
}
\arguments{
\item{field}{Field id whose visibility should be controlled.}

\item{visible}{Function returning \code{TRUE} to show the field and \code{FALSE} to hide
it. It may declare any of \code{input}, \code{context}, \code{field}, \code{prefix} and
\code{values} as arguments; \code{values} (the current field values, keyed by field
id) is the one available both in the dialog and on save, so predicates
should key off it.}

\item{depends_on}{Optional field ids that trigger re-evaluation. Defaults to
the fields the predicate reacts to; list them so the dialog updates live.}
}
\value{
An input binding object.
}
\description{
Creates an input binding for \code{\link[=form_server]{form_server()}} that shows or hides one form
field depending on the live values of other inputs. The field's container is
toggled in the add/edit dialog whenever the dialog opens or a dependency field
changes, so a field can "pop up" only when it is relevant.
}
\details{
The same predicate is re-evaluated server-side on save: when it returns
\code{FALSE} the field's value is dropped before insert/update, so a hidden field
never persists a stale value. This cannot be bypassed from the client.
}
\examples{
b <- dynamic_visibility(
  field = "reason",
  depends_on = "status",
  visible = function(values) identical(values$status, "Rejected")
)
b$field
b$depends_on
}
