% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_issues.R
\name{validation_issues}
\alias{validation_issues}
\title{List the validation issues of a record}
\usage{
validation_issues(
  form,
  record,
  conn = NULL,
  record_id = NULL,
  require_all_mandatory = TRUE
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{record}{Named list or one-row data frame.}

\item{conn}{Optional DBI connection. Required for unique checks.}

\item{record_id}{Optional \code{sft_id} of the record being updated, so its own
stored values do not count as duplicates in unique checks.}

\item{require_all_mandatory}{Logical. Whether missing mandatory fields are
treated as validation errors.}
}
\value{
A data frame with one row per issue and the columns \code{severity}
(\code{"error"} or \code{"warning"}), \code{source} (\code{"mandatory"}, \code{"unique"} or
\code{"rule:<id>"}), \code{message} and \code{fields} (a list column of field ids; empty
for a rule that names no fields). Zero rows mean the record is valid.
}
\description{
The non-throwing counterpart of \code{\link[=validate_record]{validate_record()}}: instead of stopping at
the first failed record it returns every issue with the fields it concerns,
so a script (or a custom UI) can react to them individually.
}
\examples{
f <- form(
  form_id = "contacts",
  table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(
    form_field(id = "name", label = "Name", mandatory = TRUE),
    form_field(id = "email", label = "Email")
  ),
  validation_rules = list(
    warning_if(
      id = "no_email",
      condition = function(values) !nzchar(values$email),
      fields = "email",
      message = "No email address given."
    )
  )
)

issues <- validation_issues(f, list(name = "", email = ""))
issues[, c("severity", "source", "message")]
issues$fields
}
\seealso{
\code{\link[=validate_record]{validate_record()}}, which raises the errors among these issues as
a condition of class \code{sft_validation_error} carrying the same data frame
in its \code{issues} element.
}
