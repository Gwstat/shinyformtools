% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_rules.R
\name{warning_if}
\alias{warning_if}
\title{Emit a warning when a condition is true}
\usage{
warning_if(id, condition, fields = NULL, message = NULL)
}
\arguments{
\item{id}{Stable rule id.}

\item{condition}{Function returning \code{TRUE} when a warning should be emitted.}

\item{fields}{Optional field ids associated with the warning.}

\item{message}{Optional warning message.}
}
\value{
An object of class \code{"sft_validation_rule"}.
}
\description{
Convenience wrapper around \code{\link[=forbid_if]{forbid_if()}} with \code{severity = "warning"}.
Warnings are emitted server-side but do not block insert or update.
}
\examples{
rule <- warning_if(
  id = "high_amount",
  condition = function(values) !is.null(values$amount) && values$amount > 10000,
  fields = "amount",
  message = "Amount is unusually high; please double-check."
)
rule$id
rule$severity
}
