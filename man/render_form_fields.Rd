% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_form.R
\name{render_form_fields}
\alias{render_form_fields}
\title{Render form fields}
\usage{
render_form_fields(
  form,
  ns = identity,
  prefix = "",
  values = NULL,
  read_only = FALSE,
  editable_fields = NULL
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{ns}{Namespace function. Defaults to \code{identity}.}

\item{prefix}{Optional input/output id prefix, for example \code{"add_"} or
\code{"edit_"}.}

\item{values}{Optional named list or one-row data frame used to prefill
input fields.}

\item{read_only}{Logical. Whether all input fields should be rendered disabled.}

\item{editable_fields}{Optional character vector of field ids that remain
editable; every other input field is rendered read-only. \code{NULL} keeps each
field's own \code{editable} setting.}
}
\value{
Shiny UI.
}
\description{
Renders all active fields of a form. Input fields are stored; HTML and output
fields are display-only. Layout uses \code{slide}, \code{tab}, \code{col} and \code{pos}.
}
\examples{
\dontrun{
library(shiny)
contacts <- form(
  form_id = "contacts", table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(
    form_field(id = "name", label = "Name", mandatory = TRUE),
    form_field(id = "city", label = "City")
  )
)
ui <- fluidPage(
  # Render all fields prefilled, with only "name" left editable.
  render_form_fields(
    contacts,
    prefix = "edit_",
    values = list(name = "Ada", city = "London"),
    editable_fields = "name"
  )
)
server <- function(input, output, session) {}
shinyApp(ui, server)
}
}
