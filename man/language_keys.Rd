% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/language.R
\name{language_keys}
\alias{language_keys}
\title{List every text key with its English default}
\usage{
language_keys()
}
\value{
A data frame with the columns \code{vocabulary} (\code{"labels"},
\code{"messages"} or \code{"table_labels"}), \code{key} and \code{default} (the English text;
\code{NA} for a key whose default is "not shown"). Placeholders such as
\code{{label}} are filled in by the package.
}
\description{
The reference for translating the package or renaming a single button: one
row per key, for the three keyed vocabularies of a \code{\link[=language]{language()}}. (The fourth,
\code{dt_language}, is 'DataTables' own \code{language} option and documented there.)
}
\examples{
keys <- language_keys()
head(keys[keys$vocabulary == "messages", ])
table(keys$vocabulary)
}
