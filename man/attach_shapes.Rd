% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes.R
\name{attach_shapes}
\alias{attach_shapes}
\title{Attach fixed geometries to records}
\usage{
attach_shapes(form, shapes, key, conn = NULL, shape_field = NULL, user = NULL)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{shapes}{An \code{sf} object whose rows carry the key column(s) and geometry.}

\item{key}{Named character vector mapping record (database) columns to the
\code{sf} columns that identify the same feature, e.g.
\code{c(district_id = "WKR_NR")}. An unnamed string means the column has the
same name in both.}

\item{conn}{Optional DBI connection.}

\item{shape_field}{Optional shape field id, required only when the form has
more than one shape field.}

\item{user}{Optional user identifier for the audit log.}
}
\value{
Invisibly, a list with \code{attached} (number of records updated) and
\code{unmatched} (number of \code{sf} features with no matching live record).
}
\description{
Loads geometries from an \code{sf} object into a form's shape column, matching
features to existing records by a key. Shapes are stored as serialized text
and are never editable through the form, so editing a record's input fields
never disturbs its geometry. Run after the records exist; each updated record
gets an audit-log entry.
}
\examples{
# Loads geometry from an sf object into a form's shape column, matching
# features to existing records by key (requires the sf package and that the
# records already exist in the database).
\dontrun{
frm <- form(
  form_id = "districts",
  fields = list(
    form_field("district_id", "District ID"),
    shape_field("geom", crs = 4326)
  ),
  db = db_sqlite("districts.sqlite")
)
# `shapes` is an sf object carrying a WKR_NR column and geometry:
attach_shapes(frm, shapes, key = c(district_id = "WKR_NR"))
}
}
