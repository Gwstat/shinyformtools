% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lang.R
\name{use_german}
\alias{use_german}
\title{Switch all default user-facing text to German}
\usage{
use_german()
}
\value{
Invisibly, the previous values of the affected options.
}
\description{
Sets the global options consulted by every default string, so a whole app
renders in German without passing \code{labels} / \code{messages} to each form. English
remains the package default; this opt-in switch overrides it. Explicit
per-form \code{labels} / \code{messages} arguments still take precedence, so individual
strings can be renamed situationally.
}
\examples{
# Switch the whole app to German defaults, then restore the previous state.
old <- use_german()
getOption("shinyformtools.labels")$save
options(old)
}
\seealso{
\code{\link[=use_english]{use_english()}} to clear the switch, and \code{\link[=german_labels]{german_labels()}},
\code{\link[=german_messages]{german_messages()}}, \code{\link[=german_table_labels]{german_table_labels()}} for the underlying lists.
}
