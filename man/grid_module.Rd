% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_grid.R
\name{grid_module}
\alias{grid_module}
\alias{grid_ui}
\alias{grid_server}
\title{Grid entry over several records}
\usage{
grid_ui(id, label = NULL, autosave = TRUE, width = NULL)

grid_server(
  id,
  form,
  rows,
  key,
  group = NULL,
  cols = NULL,
  conn = NULL,
  user = NULL,
  hint = NULL,
  empty = sft_grid_empty_row,
  autosave = TRUE,
  sums = TRUE,
  language = NULL,
  labels = list()
)
}
\arguments{
\item{id}{Module id.}

\item{label}{Optional label above the grid.}

\item{autosave}{Logical. \code{TRUE} saves half a second after the last change;
\code{FALSE} shows a Save button instead. Give \code{grid_ui()} and \code{grid_server()}
the same value.}

\item{width}{Optional CSS width of the grid.}

\item{form}{Object created with \code{\link[=form]{form()}}. Its fields hold the row key, the
group fields and the numeric value fields.}

\item{rows}{The rows of the grid: a character vector of row keys (names,
when given, are the labels shown), a data frame with \code{value} and \code{label}
columns, or a function / reactive returning one of these.}

\item{key}{Field id of the row key, for example \code{"party"}.}

\item{group}{Named list of field values that identify the group the grid
shows, for example \code{list(day = "fri", station = "12")}, or a function /
reactive returning one. \code{NULL} means the grid covers the whole table.}

\item{cols}{Field ids of the value columns, in this order. Default: every
numeric input field that is neither the row key nor a group field.}

\item{conn}{Optional DBI connection; see \link{connections}. Without one the
module opens its own for the session.}

\item{user}{Optional user identifier or function returning it, for the
audit log.}

\item{hint}{Optional function of the group values returning a matrix of the
grid's shape (or \code{NULL}) shown greyed inside the cells, for example the
previous day's entries. A hint is never saved.}

\item{empty}{Function of a row's values (the value columns only) returning
\code{TRUE} when the row counts as empty. Default: every cell 0 or \code{NA}.}

\item{sums}{Logical. Show row, column and total sums.}

\item{language}{Optional \code{\link[=language]{language()}} object or function / reactive
returning one.}

\item{labels}{Optional named list overriding the module's labels
(\code{grid_save}, \code{grid_saving}, \code{grid_saved}, \code{grid_sum}).}
}
\value{
\code{grid_ui()} returns the UI. \code{grid_server()} returns a list with
\code{changed} (a reactive counter incremented on every save that wrote
something), \code{value} (the grid's current matrix) and \code{reload()} (re-read
the group from the database).
}
\description{
A grid whose rows are records of a form and whose columns are the form's
numeric fields, for entering many related records at once: the parties of
one polling station, the items of one order. Each row of the grid is one
stored record, identified by a row key (\code{key}) and, usually, a group key
(\code{group}) that names the set of records the grid shows. Saving writes the
whole grid through \code{\link[=upsert_records]{upsert_records()}} in one transaction: rows that are
new are inserted, changed rows updated, unchanged rows left alone, empty
rows soft-deleted (so the table holds only what was entered), and stored
rows of the group that the grid no longer lists are soft-deleted too.
}
\details{
The grid itself is \code{\link[=grid_input]{grid_input()}}: Enter and the arrow keys walk the cells,
a block from a spreadsheet can be pasted, sums follow the entries. It is
rendered once per group (and whenever \code{rows} change), never on save, so the
cursor stays where it is while values are written half a second after the
last keystroke (\code{autosave = TRUE}) or on a Save button.

Two people editing the same group at the same time both write; the last
save of a row wins. Every write is audit-logged per record like any other.
}
\examples{
\dontrun{
library(shiny)
db <- db_sqlite(tempfile(fileext = ".sqlite"))
counts <- form(
  form_id = "counts", table_name = "counts", db = db,
  fields = list(
    form_field(id = "station", label = "Station"),
    form_field(id = "party", label = "Party"),
    form_field(id = "posters", label = "Posters", input_type = "numericInput"),
    form_field(id = "flyers", label = "Flyers", input_type = "numericInput")
  )
)
ui <- fluidPage(
  selectInput("station", "Station", c("North", "South")),
  grid_ui("counts")
)
server <- function(input, output, session) {
  grid_server(
    "counts", counts,
    rows = c("Party A", "Party B", "Party C"), key = "party",
    group = reactive(list(station = input$station)), user = "demo"
  )
}
shinyApp(ui, server)
}
}
