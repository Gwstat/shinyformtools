% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_restore.R
\name{restore_record}
\alias{restore_record}
\title{Restore a record from the audit log}
\usage{
restore_record(
  form,
  record_id = NULL,
  record_uuid = NULL,
  version_no = NULL,
  conn = NULL,
  user = NULL,
  reason = NULL,
  reactivate = TRUE
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{record_id}{Optional \code{sft_id}.}

\item{record_uuid}{Optional \code{sft_uuid}.}

\item{version_no}{Optional audit version number to restore.}

\item{conn}{Optional DBI connection.}

\item{user}{Optional user identifier.}

\item{reason}{Optional reason for audit log.}

\item{reactivate}{Logical. If \code{TRUE} (default), the restored record is marked
live (not deleted) and reclaims the live unique slot. If \code{FALSE}, the
record's field values are restored but its current deleted state is left
untouched, so a soft-deleted record stays soft-deleted.}
}
\value{
The restored record as a one-row data frame.
}
\description{
Restores a record from a stored audit snapshot. If \code{version_no} is omitted,
the latest non-deleted snapshot from an insert, update or restore action is
used.
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
conn <- db_connect(db)
init_db(contacts, conn = conn, user = "demo")
rec <- insert_record(contacts, list(name = "Ada"), conn = conn, user = "demo")
update_record(
  contacts, list(name = "Ada Lovelace"),
  record_id = rec$sft_id[1], conn = conn, user = "demo"
)

# Restore the original (version 1) field values.
restore_record(
  contacts,
  record_id = rec$sft_id[1], version_no = 1,
  conn = conn, user = "demo"
)

db_disconnect(conn)
}
