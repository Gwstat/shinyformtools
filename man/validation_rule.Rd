% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_rules.R
\name{validation_rule}
\alias{validation_rule}
\title{Define a cross-field validation rule}
\usage{
validation_rule(
  id,
  validate = NULL,
  condition = NULL,
  fields = NULL,
  severity = c("error", "warning"),
  message = NULL
)
}
\arguments{
\item{id}{Stable rule id.}

\item{validate}{Optional function returning \code{TRUE} for a valid record,
\code{FALSE} for an invalid record, or a character vector with validation
messages. Functions may declare any of \code{values}, \code{record}, \code{form}, \code{conn},
\code{current_id} and \code{context}.}

\item{condition}{Optional function used with \code{fields}. If it returns \code{TRUE},
the listed fields must be non-empty. Functions may declare any of \code{values},
\code{record}, \code{form}, \code{conn}, \code{current_id} and \code{context}.}

\item{fields}{Optional field ids checked as conditionally required when
\code{condition} is supplied.}

\item{severity}{Either \code{"error"} or \code{"warning"}. Errors block insert/update;
warnings are emitted but do not block persistence.}

\item{message}{Validation message. May be a character scalar or a function.}
}
\value{
An object of class \code{"sft_validation_rule"}.
}
\description{
Validation rules are evaluated on the server during insert and update, so they
cannot be bypassed by changing client-side state. A rule can either provide a
custom \code{validate} function, or combine \code{condition} with \code{fields} to implement
conditional required fields.
}
\examples{
rule <- validation_rule(
  id = "end_after_start",
  validate = function(values) {
    is.null(values$end) || is.null(values$start) || values$end >= values$start
  },
  message = "End date must not be before the start date."
)
rule$id
rule$severity
}
