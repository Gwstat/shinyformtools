% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_crud.R
\name{insert_record}
\alias{insert_record}
\title{Insert a form record}
\usage{
insert_record(form, record, conn = NULL, user = NULL, reason = NULL)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{record}{Named list or one-row data frame.}

\item{conn}{Optional DBI connection.}

\item{user}{Optional user identifier.}

\item{reason}{Optional reason for audit log.}
}
\value{
The inserted record as a one-row data frame.
}
\description{
Insert a form record
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(
    form_field(id = "name", label = "Name", mandatory = TRUE),
    form_field(id = "email", label = "Email", unique = TRUE)
  )
)
conn <- db_connect(db)
init_db(contacts, conn = conn, user = "demo")
insert_record(contacts, list(name = "Ada", email = "ada@example.org"),
              conn = conn, user = "demo")
db_disconnect(conn)
}
