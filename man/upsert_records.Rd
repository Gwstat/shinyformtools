% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_upsert.R
\name{upsert_records}
\alias{upsert_records}
\title{Write several records in one transaction, matched by a key}
\usage{
upsert_records(
  form,
  records,
  key,
  conn = NULL,
  user = NULL,
  reason = NULL,
  empty = NULL,
  scope = NULL
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{records}{A data frame, or a list of named lists, with one element per
record. Names are field ids (or database columns). Fields not present in
a row are left untouched on update and empty on insert.}

\item{key}{Character vector of field ids that identify a record, for
example \code{c("district", "party")}. Every row must carry them. The stored
rows are expected to be unique per key; an ambiguous match is an error.}

\item{conn}{Optional DBI connection; see \link{connections}.}

\item{user}{Optional user identifier for the audit log.}

\item{reason}{Optional reason for the audit log.}

\item{empty}{Optional function of a row's values without the key fields,
returning \code{TRUE} when the row is empty. Default \code{NULL}: no row is empty.
Example for a grid of counts: \code{function(values) all(unlist(values) \%in\% c(0, NA))}.}

\item{scope}{Optional named list of field values that describe the group
\code{records} covers completely, for example \code{list(district = "1")}. Live
records matching \code{scope} whose key is not among \code{records} are
soft-deleted.}
}
\value{
Invisibly, a data frame with one row per record (and one per scope
deletion): \code{row} (the index in \code{records}, \code{NA} for scope deletions),
\code{action} (\code{"insert"}, \code{"update"}, \code{"unchanged"}, \code{"delete"} or \code{"skip"})
and \code{sft_id} (\code{NA} for a skipped row).
}
\description{
Inserts, updates or soft-deletes the records of a form so that the stored
rows reflect \code{records}, all inside one transaction: either every row is
written or none is. Each row is matched to a live stored record by the
\code{key} fields. A row with no match is inserted; a row whose match already
holds the same values is left alone (no write, no audit entry); any other
match is updated. Every write goes through the same validation and audit
log as \code{\link[=insert_record]{insert_record()}}, \code{\link[=update_record]{update_record()}} and \code{\link[=soft_delete_record]{soft_delete_record()}}.
}
\details{
\code{empty} and \code{scope} make the function fit a grid of counts, where rows are
shown for every party or category but only what was actually entered
should be stored: \code{empty} says which rows count as empty (they are
soft-deleted if stored, and skipped otherwise), and \code{scope} names the group
the records describe completely, so that stored rows of that group missing
from \code{records} are soft-deleted as well.
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
votes <- form(
  form_id = "votes", table_name = "votes", db = db,
  fields = list(
    form_field(id = "district", label = "District"),
    form_field(id = "party", label = "Party"),
    form_field(id = "count", label = "Count", input_type = "numericInput")
  )
)
counts <- data.frame(
  district = "1", party = c("A", "B", "C"), count = c(412, 388, 0)
)
result <- upsert_records(
  votes, counts, key = c("district", "party"),
  empty = function(values) all(unlist(values) \%in\% c(0, NA)),
  scope = list(district = "1")
)
result$action
fetch_records(votes)[, c("party", "count")]
}
