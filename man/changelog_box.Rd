% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/changelog.R
\name{changelog_box}
\alias{changelog_box}
\title{Create a changelog box for modal headers}
\usage{
changelog_box(
  context,
  record = NULL,
  record_id = NULL,
  record_uuid = NULL,
  title = "Changelog",
  limit = 5L,
  empty_text = "No changes yet.",
  datetime_format = sft_default_datetime_format(),
  show_version = FALSE,
  show_fields = FALSE
)
}
\arguments{
\item{context}{Form server context as supplied to \code{modal_header} hooks.}

\item{record}{Optional one-row record containing \code{sft_id} or \code{sft_uuid}.}

\item{record_id}{Optional \code{sft_id}.}

\item{record_uuid}{Optional \code{sft_uuid}.}

\item{title}{Box title.}

\item{limit}{Maximum number of entries to show.}

\item{empty_text}{Text shown when no history is available.}

\item{datetime_format}{Timestamp display format.}

\item{show_version}{Logical. Whether to show the version number in the compact history.}

\item{show_fields}{Logical. Whether to show compact changed-field names.}
}
\value{
Shiny UI.
}
\description{
Create a changelog box for modal headers
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
conn <- db_connect(db)
init_db(contacts, conn = conn, user = "demo")
rec <- insert_record(contacts, list(name = "Ada"), conn = conn, user = "demo")

# `context` is normally supplied by a form_server() modal_header hook;
# here a plain list(form, conn) stands in for it.
context <- list(form = contacts, conn = conn)
changelog_box(context, record_id = rec$sft_id[1], title = "History")

db_disconnect(conn)
}
