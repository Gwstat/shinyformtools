% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_connect.R
\name{db_connect}
\alias{db_connect}
\title{Connect to a database}
\usage{
db_connect(db = db_sqlite())
}
\arguments{
\item{db}{Database configuration created with \code{db_sqlite()} or
\code{db_mariadb()} or \code{db_duckdb()}. A character scalar is
treated as SQLite path for backwards compatibility.}
}
\value{
A DBI connection.
}
\description{
Connect to a database
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
conn <- db_connect(db)
DBI::dbIsValid(conn)
db_disconnect(conn)
}
