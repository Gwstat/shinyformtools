% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_records}
\alias{export_records}
\title{Export form records to CSV or Excel}
\usage{
export_records(
  form,
  file = NULL,
  conn = NULL,
  format = NULL,
  include_deleted = FALSE,
  columns = NULL,
  labels = TRUE,
  system_columns = FALSE,
  datetime_format = sft_default_datetime_format()
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{file}{Path of the file to write. \code{NULL} writes nothing and only
returns the prepared data, for example to hand it to another writer.}

\item{conn}{Optional DBI connection; see \link{connections}.}

\item{format}{\code{"csv"}, \code{"csv2"} or \code{"xlsx"}. The default \code{NULL} takes it
from the extension of \code{file}. \code{"xlsx"} needs the \pkg{openxlsx} package.}

\item{include_deleted}{Logical. Whether soft-deleted records are included.
\code{"only"} exports just the soft-deleted records.}

\item{columns}{Optional character vector of columns (database names) to
export, in this order. The default is what the records table shows.}

\item{labels}{Logical. \code{TRUE} uses the field labels as column headers,
\code{FALSE} the database column names, which is the better choice when the
file is read by a program again.}

\item{system_columns}{Logical. Whether the extended \code{sft_} columns (created
and updated by / at, deletion state) are exported too.}

\item{datetime_format}{Format for timestamps, as in \code{\link[=records_datatable]{records_datatable()}}.}
}
\value{
The exported data frame, invisibly.
}
\description{
Writes the records of a form to a file that opens cleanly in a spreadsheet:
field labels as column headers, multi-value fields as readable text instead
of JSON arrays, numbers as numbers, checkboxes as \code{TRUE} / \code{FALSE}, and
timestamps in local time. Shape columns and the internal system columns are
left out, like in the records table.
}
\details{
CSV files are written as UTF-8 with a byte order mark, which is what Excel
needs to read umlauts and other non-ASCII text correctly. Use
\code{format = "csv2"} for the semicolon-separated dialect with a decimal comma
that Excel expects on German and most other European systems.
}
\examples{
db <- db_sqlite(tempfile(fileext = ".sqlite"))
contacts <- form(
  form_id = "contacts", table_name = "contacts", db = db,
  fields = list(
    form_field(id = "name", label = "Name"),
    form_field(id = "age", label = "Age", input_type = "numericInput")
  )
)
insert_record(contacts, list(name = "Ada", age = 36))

target <- tempfile(fileext = ".csv")
export_records(contacts, target)
readLines(target)
}
