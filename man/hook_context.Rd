% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topics.R
\name{hook_context}
\alias{hook_context}
\title{The \code{context} object passed to hooks}
\description{
Several arguments of \code{\link[=form_server]{form_server()}} take a function that the module calls
while it runs: \code{display_transform}, \code{modal_header}, \code{table$format} and the
handlers of \code{input_bindings}. When such a function declares an argument named
\code{context}, it receives the list described here. It gives the hook access to
the form, the database and the module's current state, so that it can look
up related records or react to the selected row.
}
\section{Elements}{

\describe{
\item{\code{form}}{The form object the module was started with.}
\item{\code{conn}}{The module's database connection, or \code{NULL} when the
database cannot be reached at the moment. Use it for your own queries,
for example \code{fetch_records(other_form, conn = context$conn)}. Do not
close it.}
\item{\code{user}}{The current user name as a string, as resolved from the
\code{user} argument.}
\item{\code{records}}{A reactive: the records as fetched from the database.
Call it as \code{context$records()}.}
\item{\code{display_records}}{A reactive: the records after
\code{display_transform}, which is what the table shows. Do not call it
inside \code{display_transform} itself.}
\item{\code{selected_record}}{A reactive: the one-row data frame selected in
the table, or \code{NULL}.}
\item{\code{refresh}}{A function without arguments that makes the module fetch
its records again.}
\item{\code{input}, \code{output}, \code{session}}{The module's own Shiny objects. Input
ids are already namespaced, so \code{context$input$open_add} is the module's
add button.}
}
}

\section{Reactivity}{

The reactives are handed over uncalled. A hook that calls one inside a
reactive context, which is where the module runs its hooks, takes a
dependency on it and runs again when the value changes.
}

\examples{
# A display_transform that replaces a stored id by a name from another form.
show_department <- function(data, context) {
  departments <- fetch_records(department_form, conn = context$conn)
  data$department <- departments$name[match(data$department, departments$sft_id)]
  data
}
}
\seealso{
\code{\link[=form_server]{form_server()}}
}
