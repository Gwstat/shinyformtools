% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field.R
\name{form_field}
\alias{form_field}
\title{Define a form field}
\usage{
form_field(
  id,
  label = NULL,
  type = "input",
  input_type = NULL,
  args = list(),
  db_column = NULL,
  db_type = NULL,
  db_default = NULL,
  mandatory = FALSE,
  unique = FALSE,
  editable = TRUE,
  markdown = FALSE,
  show = TRUE,
  tab = 0L,
  tab_label = NULL,
  slide = 0L,
  slide_label = NULL,
  col = 0L,
  pos = 0L,
  status = "active",
  renamed_from = NULL
)
}
\arguments{
\item{id}{Stable field identifier.}

\item{label}{User-facing field label.}

\item{type}{Field type. One of \code{"input"}, \code{"html"}, \code{"text_output"},
\code{"plot_output"} or \code{"ui_output"}.}

\item{input_type}{Shiny input function name for input fields.}

\item{args}{List of arguments passed to the input/output function.}

\item{db_column}{Database column name. Defaults to \code{renamed_from} when
supplied, otherwise to \code{id}.}

\item{db_type}{Database column type. If \code{NULL}, a conservative default
is derived from \code{input_type}.}

\item{db_default}{Optional database default value.}

\item{mandatory}{Logical. Whether the field is required.}

\item{unique}{Logical. Whether values in this field must be unique.}

\item{editable}{Logical, or a function of the current user
(\verb{function(user)}) returning a logical. When \code{FALSE} (or the function
returns \code{FALSE} for the current user) the field is shown disabled and is
dropped server-side on both add and edit, so it cannot be written. A
function is resolved per user at render and save time; it must be on an
input field. Errors in the function are treated as not editable
(fail-closed).}

\item{markdown}{Logical. When \code{TRUE}, the field's stored value is rendered as
Markdown in the records and versions tables instead of being shown as plain
text. The value is HTML-escaped before rendering and the output is
URL-sanitized so that user-entered content is not rendered as active scripts
or unsafe links. Editing still shows the raw Markdown source. Only input
fields may be markdown. Requires the \pkg{commonmark} package.}

\item{show}{Logical. Whether the field is shown in the form.}

\item{tab}{Integer-like layout tab position.}

\item{tab_label}{Optional label for the tab this field belongs to.}

\item{slide}{Integer-like layout slide position.}

\item{slide_label}{Optional label for the slide this field belongs to.}

\item{col}{Integer-like layout column position.}

\item{pos}{Integer-like position inside tab/slide/column.}

\item{status}{Field lifecycle status. One of \code{"active"}, \code{"retired"} or
\code{"orphaned"}.}

\item{renamed_from}{Optional previous field id if this field is an explicit
rename. If \code{db_column} is omitted, the previous id is reused as database
column so existing values remain available.}
}
\value{
A field definition object of class \code{"sft_field"}.
}
\description{
Creates a field definition for a shinyformtools form.
}
\examples{
# A plain text input field.
name <- form_field(id = "name", label = "Name", mandatory = TRUE)

# A select input with a unique constraint.
team <- form_field(
  id = "team", label = "Team", input_type = "selectInput",
  args = list(choices = c("Sales", "Support")), unique = TRUE
)
name$db_column
}
