% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_table.R
\name{records_datatable}
\alias{records_datatable}
\title{Create a DT table for form records}
\usage{
records_datatable(
  data,
  form,
  columns = NULL,
  show_system_columns = FALSE,
  selection = "single",
  options = list(),
  class = "display compact stripe hover nowrap",
  datetime_format = sft_default_datetime_format(),
  display_column_labels = NULL,
  filter = "none"
)
}
\arguments{
\item{data}{Data frame returned by \code{\link[=fetch_records]{fetch_records()}}.}

\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{columns}{Optional character vector of columns to display.}

\item{show_system_columns}{Logical. Whether extended \code{sft_} columns are shown.}

\item{selection}{DT row selection mode.}

\item{options}{Additional DT options.}

\item{class}{CSS class passed to \code{\link[DT:datatable]{DT::datatable()}}.}

\item{datetime_format}{Format used for displayed timestamps.}

\item{display_column_labels}{Optional named character vector with labels for
additional display-only columns created by a display transform.}

\item{filter}{Passed to \code{\link[DT:datatable]{DT::datatable()}}'s \code{filter} argument. \code{"none"}
(default), \code{"top"} or \code{"bottom"} add per-column search controls that adapt
to each column's type (range slider for numeric columns, a select for
factors, a text box otherwise).}
}
\value{
A DT table widget.
}
\description{
Create a DT table for form records
}
\examples{
contacts <- form(
  form_id = "contacts", table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(
    form_field(id = "name", label = "Name"),
    form_field(id = "city", label = "City")
  )
)
records <- data.frame(
  name = c("Ada", "Linus"),
  city = c("London", "Helsinki")
)
records_datatable(records, form = contacts)
}
