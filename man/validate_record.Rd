% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_record}
\alias{validate_record}
\title{Validate a record against a form schema}
\usage{
validate_record(
  form,
  record,
  conn = NULL,
  record_id = NULL,
  require_all_mandatory = TRUE,
  current_id = NULL
)
}
\arguments{
\item{form}{Object created with \code{\link[=form]{form()}}.}

\item{record}{Named list or one-row data frame.}

\item{conn}{Optional DBI connection. Required for unique checks.}

\item{record_id}{Optional \code{sft_id} of the record being updated, so its own
stored values do not count as duplicates in unique checks.}

\item{require_all_mandatory}{Logical. Whether missing mandatory fields are
treated as validation errors.}

\item{current_id}{Deprecated name of \code{record_id}; accepted with a warning.}
}
\value{
Invisibly returns \code{TRUE}. A record with errors raises a condition of
class \code{sft_validation_error}: its message lists every error, one per line,
and its \code{issues} element is the data frame \code{\link[=validation_issues]{validation_issues()}} returns
(restricted to errors), so a caller can tell which fields failed. Rules of
severity \code{"warning"} raise a warning and do not stop.
}
\description{
Validate a record against a form schema
}
\examples{
f <- form(
  form_id = "contacts",
  table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(
    form_field(id = "name", label = "Name", mandatory = TRUE),
    form_field(id = "email", label = "Email")
  )
)
# A complete record passes (mandatory-field check needs no database).
validate_record(f, list(name = "Ada", email = "ada@example.com"))

# A missing mandatory field raises an error.
try(validate_record(f, list(email = "ada@example.com")))
}
\seealso{
\code{\link[=validation_issues]{validation_issues()}} for the non-throwing variant.
}
