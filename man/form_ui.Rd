% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_form.R
\name{form_ui}
\alias{form_ui}
\title{Form module UI}
\usage{
form_ui(
  id,
  title = NULL,
  show_user = TRUE,
  show_audit = TRUE,
  show_include_deleted = FALSE,
  show_add = TRUE,
  show_edit = TRUE,
  show_deleted_records = TRUE,
  show_delete = TRUE,
  show_refresh_table = TRUE,
  show_versions = FALSE,
  show_column_settings = TRUE,
  show_column_selection = TRUE,
  form_layout = c("modal", "inline"),
  labels = list(),
  button_options = list()
)
}
\arguments{
\item{id}{Module id.}

\item{title}{Optional title shown above the module.}

\item{show_user}{Logical. Whether to show a user input field.}

\item{show_audit}{Logical. Whether to show the audit log table.}

\item{show_include_deleted}{Logical. Whether to show the legacy include-deleted
checkbox.}

\item{show_add}{Logical. Whether to show the add button.}

\item{show_edit}{Logical. Whether to show the edit button.}

\item{show_deleted_records}{Logical. Whether to show the deleted-records dialog button.}

\item{show_delete}{Logical. Whether to show the delete button.}

\item{show_refresh_table}{Logical. Whether to show a button that clears table filters, ordering, paging and row selection.}

\item{show_versions}{Deprecated compatibility flag. Standalone version buttons are no longer rendered by the default UI; versions are shown in the case modal when permitted.}

\item{show_column_settings}{Logical. Whether to show the admin column-settings button.}

\item{show_column_selection}{Logical. Whether to show the user column-selection button.}

\item{form_layout}{Where the add/edit forms render. \code{"modal"} (default) opens
them in a dialog; \code{"inline"} renders them in a panel above the records table,
with add and edit mutually exclusive. Must match the \code{form_layout} passed to
\code{\link[=form_server]{form_server()}}.}

\item{labels}{Optional named list overriding UI labels and button texts.}

\item{button_options}{Optional named list controlling action-button placement
and classes. Supported entries are \code{placement} (\code{"top"}, \code{"bottom"},
\code{"both"}, \code{"none"}), \code{align} (\code{"left"}, \code{"center"},
\code{"right"}, \code{"between"}), \code{class}, \code{container_class},
\code{container_style} and \code{button_classes}.}
}
\value{
Shiny UI.
}
\description{
Form module UI
}
\examples{
\dontrun{
library(shiny)
contacts <- form(
  form_id = "contacts", table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
ui <- fluidPage(form_ui("contacts", title = "Contacts"))
server <- function(input, output, session) {
  form_server(id = "contacts", form = contacts)
}
shinyApp(ui, server)
}
}
