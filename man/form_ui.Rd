% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_form.R
\name{form_ui}
\alias{form_ui}
\title{Form module UI}
\usage{
form_ui(
  id,
  title = NULL,
  show_user = FALSE,
  show_audit = FALSE,
  show_add = TRUE,
  show_edit = TRUE,
  show_deleted_records = FALSE,
  show_delete = TRUE,
  show_refresh_table = TRUE,
  show_column_settings = FALSE,
  show_column_selection = FALSE,
  form_layout = c("modal", "inline"),
  table_style = NULL,
  labels = list(),
  button_options = list(),
  language = NULL,
  ...
)
}
\arguments{
\item{id}{Module id.}

\item{title}{Optional title shown above the module.}

\item{show_user}{Logical. Whether to show a user input field. Off by default:
in most apps the acting user comes from authentication, not a text box.}

\item{show_audit}{Logical. Whether to show the audit log table. Off by
default. Every mutation is still recorded either way; this only controls
whether the log is displayed. Requires the \code{can_view_audit} permission.}

\item{show_add}{Logical. Whether to show the add button.}

\item{show_edit}{Logical. Whether to show the edit button.}

\item{show_deleted_records}{Logical. Whether to show the deleted-records
dialog button, from which soft-deleted records can be restored. Off by
default. Records are still only ever soft-deleted; this only controls
whether the restore dialog is reachable.}

\item{show_delete}{Logical. Whether to show the delete button.}

\item{show_refresh_table}{Logical. Whether to show a button that clears table filters, ordering, paging and row selection.}

\item{show_column_settings}{Logical. Whether to show the admin
column-settings button. Off by default.}

\item{show_column_selection}{Logical. Whether to show the user
column-selection button. Off by default.}

\item{form_layout}{Where the add/edit forms render. \code{"modal"} (default) opens
them in a dialog; \code{"inline"} renders them in a panel above the records table,
with add and edit mutually exclusive. \code{\link[=form_server]{form_server()}} picks the layout up
from the UI, so it only has to be set here.}

\item{table_style}{Visual preset for the module's tables (records, audit,
deleted records, versions). One of \code{"classic"} (the unmodified 'DT' look),
\code{"clean"} (card-style with row separators instead of stripes),
\code{"publication"} (a booktabs-style journal table: serif type, horizontal
rules only, no stripes) or \code{"compact"} (dense, dark header). The default \code{NULL}
consults \code{getOption("shinyformtools.table_style")} and falls back to
\code{"classic"}, so an app can set the look once and override it per form.
Purely cosmetic CSS, scoped to this module's tables; host-app tables are
unaffected.}

\item{labels}{Optional named list overriding UI labels and button texts.}

\item{button_options}{Optional named list controlling action-button placement
and classes. Supported entries are \code{placement} (\code{"top"}, \code{"bottom"},
\code{"both"}, \code{"none"}), \code{align} (\code{"left"}, \code{"center"},
\code{"right"}, \code{"between"}), \code{class}, \code{container_class},
\code{container_style} and \code{button_classes}.}

\item{language}{Optional \code{\link[=language]{language()}} object, e.g. \code{\link[=german]{german()}}: every text of
this form in one argument, for this form only. Pass the same object to
\code{\link[=form_server]{form_server()}}. Entries of \code{labels} still win over it.}

\item{...}{Deprecated arguments, accepted with a warning for one release:
\code{show_include_deleted} (the legacy include-deleted checkbox; use
\code{show_deleted_records} instead) and \code{show_versions} (no effect; versions
are shown inside the edit dialog when permitted).}
}
\value{
Shiny UI.
}
\description{
The defaults are deliberately lean: the records table and the add / edit /
delete buttons, and nothing else. Everything beyond that CRUD core -- the
audit log, the deleted-records dialog, the column buttons and the user field
-- is opt-in, so a plain \code{form_ui(id)} yields a plain table rather than a
wall of controls. Switch on what the app needs with the \verb{show_*} arguments.
}
\examples{
\dontrun{
library(shiny)
contacts <- form(
  form_id = "contacts", table_name = "contacts",
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  fields = list(form_field(id = "name", label = "Name", mandatory = TRUE))
)
ui <- fluidPage(form_ui("contacts", title = "Contacts"))
server <- function(input, output, session) {
  form_server(id = "contacts", form = contacts)
}
shinyApp(ui, server)
}
}
