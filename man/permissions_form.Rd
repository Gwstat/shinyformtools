% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{permissions_form}
\alias{permissions_form}
\title{A ready-made rights table as a shinyformtools form}
\usage{
permissions_form(
  form_ids,
  db = NULL,
  db_path = NULL,
  users = character(),
  permissions = NULL,
  form_id = "permissions",
  table_name = "permissions",
  form_name = "Permissions",
  permission_labels = NULL
)
}
\arguments{
\item{form_ids}{Character vector of form ids a rule may apply to (the choices
of the \code{forms} multi-select).}

\item{db, db_path}{Database spec or path, as in \code{\link[=form]{form()}}. Supply one.}

\item{users}{Character vector of user names for the \code{user} field choices,
for example from \code{\link[=shinymanager_users]{shinymanager_users()}}. Choices can also be refreshed at
runtime with a \code{dynamic_choices()} input binding.}

\item{permissions}{Character vector of permission field names. Defaults to
the full set understood by \code{\link[=form_server]{form_server()}}.}

\item{form_id, table_name, form_name}{Identifiers for the rights form/table.}

\item{permission_labels}{Optional named vector overriding the checkbox labels
(names are permission fields).}
}
\value{
An \code{sft_form} object.
}
\description{
Build a declarative form whose records are \emph{permission rules}: one row grants
a set of permissions to one user across one or more forms. This replaces the
"one credential column per permission per table" approach - adding a new
table just adds an entry to the \code{forms} multi-select, never a new column.
}
\details{
Each rule has a \code{user}, a \code{forms} field (a multi-select of form ids, stored
natively as a JSON array), and one checkbox per permission. Resolve the rules
for a given user and form with \code{\link[=rights_permissions]{rights_permissions()}} and spread the result
into \code{\link[=form_server]{form_server()}}. Because the rights table is an ordinary form, it is
audited, soft-deletable, and reactive, and you can render it anywhere with
\code{\link[=form_ui]{form_ui()}} / \code{\link[=form_server]{form_server()}} (for example an admin-only tab).
}
\examples{
# Build a rights table covering two data forms, backed by a temporary DB.
rights <- permissions_form(
  form_ids = c("people", "locations"),
  db = db_sqlite(tempfile(fileext = ".sqlite")),
  users = c("alice", "bob")
)
rights$form_id
vapply(rights$fields, function(f) f$id, character(1))
}
\seealso{
\code{\link[=rights_permissions]{rights_permissions()}}, \code{\link[=shinymanager_users]{shinymanager_users()}}
}
