% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/language.R
\name{language}
\alias{language}
\alias{german}
\alias{english}
\title{Bundle the user-facing text of a form into one language}
\usage{
language(
  labels = list(),
  messages = list(),
  table_labels = list(),
  dt_language = list()
)

german()

english()
}
\arguments{
\item{labels, messages, table_labels}{Named lists overriding entries of the
respective vocabulary. Text may contain \code{{placeholders}} exactly like the
defaults do.}

\item{dt_language}{Named list passed to 'DataTables' as its \code{language}
option.}
}
\value{
An object of class \code{sft_language}. \code{german()} returns the bundled
German pack as one such object. \code{english()} returns the English defaults
spelled out in full, which is what it takes to pin one form to English while
\code{\link[=use_german]{use_german()}} is active for the rest of the process.
}
\description{
All text the package shows comes from four vocabularies: \code{labels} (buttons,
dialog titles, notifications), \code{messages} (validation messages),
\code{table_labels} (system-column headers, audit actions, Yes/No, changelog
words) and \code{dt_language} (the 'DataTables' search box, pagination and info
line). \code{language()} bundles overrides for all four so they can be passed to
\code{\link[=form_ui]{form_ui()}} and \code{\link[=form_server]{form_server()}} as one argument. Unlike \code{\link[=use_german]{use_german()}}, which
switches the whole R process, a language given to a form applies to that form
only - so two sessions of one app can speak different languages.
}
\details{
Every entry is optional: what a language does not name falls back to the
global option (if any) and then to the English default. \code{\link[=language_keys]{language_keys()}}
lists every key with its default text. A \code{labels} or \code{messages} argument
given to a function directly still wins over the language.
}
\section{Switching the language while the app runs}{

\code{form_server(language = )} also accepts a function or reactive that returns
a language, for example \code{reactive(if (input$lang == "de") german() else english())}. Tables, dialogs, notifications and validation messages then
follow it at once. The buttons and titles drawn by \code{\link[=form_ui]{form_ui()}} are static
HTML: call \code{form_ui()} inside a \code{renderUI()} that reads the same reactive so
that they are redrawn too. A dialog that is already open keeps its language.
}

\examples{
# Start from German and rename one button.
de <- german()
de$labels$open_add <- "Neuer Kontakt"

# A small language of your own: everything else stays English.
short <- language(
  labels = list(open_add = "New", save = "OK"),
  messages = list(unique = "'{label}' exists already.")
)

\dontrun{
ui <- fluidPage(form_ui("contacts", language = de))
server <- function(input, output, session) {
  form_server("contacts", contacts_form, language = de)
}
}
}
\seealso{
\code{\link[=language_keys]{language_keys()}}, \code{\link[=use_german]{use_german()}}
}
