% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_bindings.R
\name{dynamic_choices}
\alias{dynamic_choices}
\title{Define dynamic choices for a form input}
\usage{
dynamic_choices(
  field,
  choices,
  depends_on = NULL,
  selected = "preserve",
  update_args = list()
)
}
\arguments{
\item{field}{Field id whose choices should be updated.}

\item{choices}{Function returning choices. It may declare any of \code{input},
\code{context}, \code{field}, \code{prefix} and \code{values} as arguments. Return a vector, a
named vector, or a data frame with \code{value} and \code{label} columns.}

\item{depends_on}{Optional field ids that trigger recomputation.}

\item{selected}{Selection policy. \code{"preserve"} keeps the current value if it
remains valid and otherwise selects the first available value. \code{"first"}
always selects the first value, and \code{"none"} clears the selection. A
function may be supplied for custom logic.}

\item{update_args}{Additional arguments passed to the relevant Shiny update
function, for example \code{list(server = TRUE, options = list(create = TRUE))}
for \code{selectizeInput}.}
}
\value{
An input binding object.
}
\description{
Creates an input binding for \code{\link[=form_server]{form_server()}} that updates the choices of
one select-like input whenever the add/edit dialog opens or dependency fields
change. This replaces legacy string-based \code{observeEvent()} snippets for
dependent choice lists.
}
\examples{
b <- dynamic_choices(
  field = "city",
  depends_on = "country",
  choices = function(values) {
    if (identical(values$country, "DE")) c("Berlin", "Munich") else c("Paris", "Lyon")
  }
)
b$field
b$depends_on
}
