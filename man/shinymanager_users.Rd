% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{shinymanager_users}
\alias{shinymanager_users}
\title{User names from a shinymanager credential database}
\usage{
shinymanager_users(db, passphrase = NULL, user_field = "user")
}
\arguments{
\item{db}{Path to the encrypted credential SQLite database, or a data frame
of credentials (the latter is handy for tests).}

\item{passphrase}{Passphrase for the encrypted database (ignored for a data
frame).}

\item{user_field}{Name of the user column.}
}
\value{
A sorted character vector of unique user names.
}
\description{
Read the user column from a shinymanager credential database (or a plain
credentials data frame) for use as the \code{users} choices of a
\code{\link[=permissions_form]{permissions_form()}}.
}
\examples{
# From a plain credentials data frame (handy for tests / non-shinymanager use):
creds <- data.frame(user = c("bob", "alice", "alice"), stringsAsFactors = FALSE)
shinymanager_users(creds)

# From an encrypted shinymanager credential database on disk:
\dontrun{
shinymanager_users("credentials.sqlite", passphrase = "secret")
}
}
\seealso{
\code{\link[=permissions_form]{permissions_form()}}, \code{\link[=shinymanager_permissions]{shinymanager_permissions()}}
}
