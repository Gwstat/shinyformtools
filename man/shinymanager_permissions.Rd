% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permissions.R
\name{shinymanager_permissions}
\alias{shinymanager_permissions}
\title{Map a shinymanager auth object to form_server() permissions}
\usage{
shinymanager_permissions(
  auth,
  mapping = NULL,
  default = FALSE,
  user_field = "user",
  user_default = NA_character_
)
}
\arguments{
\item{auth}{The object returned by \code{shinymanager::secure_server()} (a
\code{reactiveValues}). Any object that supports \code{[[} lookup of the credential
columns is accepted, which makes the adapter testable without shinymanager.}

\item{mapping}{Optional named character vector or list remapping permission
names (and \code{user}) to credential column names. Names are permission fields;
values are the columns to read. Unmapped permissions read the same-named
column.}

\item{default}{Logical fallback used when a credential column is absent or
\code{NA}. Defaults to \code{FALSE} (deny by default).}

\item{user_field}{Name of the credential column holding the user identifier.}

\item{user_default}{User identifier returned when the user column is empty.}
}
\value{
A named list of functions suitable for spreading into
\code{\link[=form_server]{form_server()}}.
}
\description{
Turn the authentication result returned by \code{shinymanager::secure_server()}
into a list of permission functions ready to spread into one or more
\code{\link[=form_server]{form_server()}} calls with \code{do.call()}. This keeps the per-user wiring in
one place instead of hand-writing a closure for every \verb{can_*} argument of
every form, which matters when several tables are driven by the same login.
}
\details{
Each permission reads a credential column from \code{auth} reactively, so the
returned functions stay in sync as \code{auth} updates. A column is treated as
granted when its value is truthy (\code{TRUE}, \code{"TRUE"}, \code{"1"}, \code{"yes"}, ...);
a missing column falls back to \code{default}.

The result is a named list whose names match the arguments of
\code{\link[=form_server]{form_server()}} (\code{user}, \code{can_add}, \code{can_view_record}, \code{can_edit},
\code{can_delete}, \code{can_restore}, \code{can_view_versions}, \code{can_view_deleted_records},
\code{can_change_column_settings}, \code{can_select_column_view}, \code{can_view_audit}), so
it can be spread directly:
\code{do.call(form_server, c(list(id = "x", form = form), perms))}.

To drive several tables from one login, call the adapter once and spread the
same \code{perms} into each form. For table-specific rights, pass a \code{mapping} that
points a permission at a table-specific credential column, for example
\code{mapping = c(can_edit = "can_edit_locations")}.
}
\examples{
# The adapter accepts any object supporting `[[` lookup, so a plain named
# list stands in for a shinymanager auth object outside a Shiny session.
auth <- list(user = "alice", can_add = "TRUE", can_edit = "FALSE")
perms <- shinymanager_permissions(auth)
perms$user()
perms$can_add()
perms$can_edit()
# Point a permission at a table-specific credential column:
auth2 <- list(user = "bob", can_edit_locations = "yes")
perms2 <- shinymanager_permissions(auth2, mapping = c(can_edit = "can_edit_locations"))
perms2$can_edit()
}
\seealso{
\code{\link[=form_server]{form_server()}}
}
